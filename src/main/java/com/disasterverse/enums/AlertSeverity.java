package com.disasterverse.enums;

/**
 * Represents the severity level of a disaster alert.
 * Mirrors DisasterAlert.Severity — used for OpenAPI docs and external references.
 */
public enum AlertSeverity {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}