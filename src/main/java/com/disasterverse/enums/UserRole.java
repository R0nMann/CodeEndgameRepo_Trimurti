package com.disasterverse.enums;

/**
 * Represents roles assigned to users in the DisasterVerse system.
 * Mirrors User.Role — used for external references, Swagger docs, and future decoupling.
 */
public enum UserRole {
    USER,
    ADMIN
}