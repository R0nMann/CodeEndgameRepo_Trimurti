package com.disasterverse.enums;

/**
 * Represents the lifecycle status of an SOS report.
 * Mirrors SosReport.SosStatus — used for OpenAPI docs and external references.
 */
public enum SosStatus {
    PENDING,
    ACTIVE,
    RESOLVED
}